@layout('layouts/main')
@set('title', 'Connexion')

@section('body')
@!navbar()
<div class="bg-grey-bg pt-16">
    @dashboard.card()
    @if(flashMessages.has('auth.errors'))
    <p class="col-span-12">
      Aucun utilisateur n'a été trouvé avec ces identifiants.
    </p>
  @end
  <form class="col-span-12" up-submit="false" action="{{route('AuthController.login')}}" method="post">
      {{ csrfField() }}
    <h2 class="text-center text-xl">Se connecter</h2>
      @!input({text:'Email',name:'email'})
      @!input({text:'Mot de passe',name:'password',type:'password'})
      <input class="mx-auto" type="submit" value="Connexion">
  </form>

  <a class="col-span-6" href="{{route('AuthController.showRegisterForm')}}">S'inscrire</a>
  <a class="col-span-6" href="{{route('AuthController.showForgotPassword')}}">Mot de passe oublié</a></div>
    @end
</div>
@end