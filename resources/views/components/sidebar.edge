@set('active','flex items-center px-4 py-2 text-gray-700 bg-gray-200 rounded-md')
@set('notactive','flex items-center px-4 py-2 text-gray-600 transition-colors duration-200 transform rounded-md hover:bg-gray-200  hover:text-gray-700')
<button class="fixed z-30 block p-2 rounded-md bg-primary md:hidden top-5 right-5" x-on:click="open = !open" x-text="open ? 'Fermer' : 'Menu'"></button>
<div class="fixed z-20 flex-col w-screen h-screen px-4 py-8 bg-white border-r md:w-96 md:static md:flex" x-bind:class="open ? '' : 'hidden'">
    <a href="{{route('SearchesController.showSearchForm')}}" class="text-3xl font-semibold text-center text-gray-800">SaveurBelge</a>
    <div class="flex flex-col justify-between flex-1 mt-6">
        <nav>
            @if(auth.user.status > 0 )
            <a class="{{request.url() == route('DashboardController.showAddShop') ? active : notactive}}" href="{{route('DashboardController.showAddShop')}}">
                @!icons.plus()
                <span class="mx-4 font-medium">Ajouter un commerce</span>
            </a>
            <hr class="my-6 border-gray-200" />
            @endif
            @each(s in request.requestData.shops)
            <a class="{{request.url() == route('DashboardController.showShop',{id:s.id}) ? active : notactive}}" href="{{route('DashboardController.showShop',{id:s.id})}}">
                @!icons.shop()
                <span class="mx-4 font-medium">{{s.name}}</span>
            </a>
            @end
            @if(request.requestData.shops.length >0 )
            <hr class="my-6 border-gray-200" />
            @endif
            <a class="{{request.url() == route('DashboardController.showAccount') ? active : notactive}}" href="{{route('DashboardController.showAccount')}}">
              @!icons.account()
              <span class="mx-4 font-medium">Mon compte</span>
          </a>
          <a class="{{notactive}}" href="{{route('AuthController.logout')}}">
            @!icons.logout()
            <span class="mx-4 font-medium">Déconnexion</span>
        </a>
        </nav>

        <div class="flex flex-col items-center px-4 -mx-2">
            <h4 class="mx-2 font-medium text-gray-800">{{auth.user.firstName}} {{auth.user.lastName}}</h4>
            <p>&copy; Loak.studio 2022</p>
        </div>
    </div>
</div>