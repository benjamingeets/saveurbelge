<div class="bg-white p-5 rounded-md xl:max-w-md w-full relative grid grid-cols-12 gap-2 mx-auto">
    @if(title)
    <h2 class="text-2xl col-span-12">{{title}}</h2>
    @endif
    @if(editHref)
    <a class="absolute right-5 top-5 hover:bg-gray-200 rounded-md" href="{{editHref}}">
        @!component('components/icons/edit')
    </a>
    @endif
    {{{ await $slots.main() }}}
</div>