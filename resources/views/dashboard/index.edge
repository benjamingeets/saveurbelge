@layout('layouts/main')
@set('title', 'Users')

@section('body')
    @card()
    <span>
        @!icons.shop()
    </span>
    <h2>Pas encore de commerce</h2>
    <p>Vous n'avez pas encore de commerce
        @if(auth.user.status > 0)
        <a href="{{route('DashboardController.showAddShop')}}">ajoutez en un</a>
        @else
        confirmez votre compte pour en ajouter un
        @endif
    </p>
    @end
@end