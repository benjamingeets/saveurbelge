<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <script src="https://unpkg.com/htmx.org@1.6.1"></script>
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body hx-boost="true">

  <main>
    <div> 
        @if(auth.isLoggedIn && auth.user.status == 5)
          <a style="position: absolute;top:0;left:0;" href="{{route('AdminsController.showDashboard')}}">Admin</a>
        @endif
        @!section('body')
    </div>
  </main>
</body>
</html>
