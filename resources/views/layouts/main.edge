<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body>
  @if(auth.isLoggedIn && auth.user.status == 5)
    <a href="{{route('AdminsController.showDashboard')}}">Admin</a>
  @endif
  @!section('body')
</body>
</html>
